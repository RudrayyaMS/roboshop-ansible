- name: Update prometheus Configuration
  ansible.builtin.templates:
    src: prometheus.yml
    dst: /opt/prometheus/prometheus.yml

- name: Restart prometheus
  ansible.builtin.systemd:
    name: prometheus
    state: installed